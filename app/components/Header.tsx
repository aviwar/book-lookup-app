const Header: React.FC = () => {
    return (
        <header className="fixed top-0 left-0 w-full bg-white shadow-md p-4">
            <span className="text-xl font-semibold">Book Lookup</span>
        </header>
    )
};

export default Header;
